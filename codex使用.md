先不要修改代码。

你正在处理一个基于 Java 21、Spring Boot 3.3、Spring Cloud、Spring AI、Milvus 的微服务知识库项目。请先阅读当前仓库中的真实代码、配置文件和启动类，不要只依赖 README。

你的任务是完成“项目启动测试 + 问题定位 + 最小改动修复 + 复测验证”的完整闭环。

请严格按下面流程执行：

第一阶段：只读分析
1. 识别项目的微服务模块及职责
2. 找出项目启动所需的最小依赖条件
3. 找出本地启动所需的关键环境变量和配置
4. 找出服务启动顺序
5. 找出最小可运行链路
6. 找出最容易导致启动失败的风险点

分析时重点关注：
- eureka-server
- config-server
- document-service
- embedding-service
- rag-service
- milvus-service
- api-gateway
- common

重点检查以下配置或风险点：
- DashScope / OpenAI 兼容模式配置
- DASHSCOPE_API_KEY
- Milvus 连接配置
- Redis 是否为可选依赖
- @ConditionalOnBean 相关条件装配
- spring.main.allow-bean-definition-overriding
- Spring Cloud Config / Eureka 注册依赖
- 各服务端口冲突
- 本地运行与测试命令

输出要求：
1. 服务启动顺序
2. 最小启动依赖
3. 必要环境变量
4. 最小验证方案
5. 高风险点清单

在完成分析之前，不要修改代码。

第二阶段：执行启动测试
完成上面的分析后，开始执行启动测试，要求如下：

1. 先执行编译或最小构建验证
2. 再按正确顺序启动服务
3. 每启动一个服务，都检查日志中的报错、异常、Bean 注入失败、配置缺失、连接失败、端口冲突等问题
4. 如果启动失败，先定位真实根因，再做修复
5. 每次只解决当前阻塞启动的根因，不处理无关问题
6. 修复后必须重新运行验证，确认是否进入下一个阻塞点
7. 不要做大规模重构
8. 不要引入新框架
9. 不要随意重命名类、方法、包结构
10. 保持微服务边界清晰，不要把一个服务的逻辑乱移到另一个服务

第三阶段：修复约束
如果需要修改代码，必须遵守以下约束：

- 只做最小、可回滚、高置信度修改
- 优先复用现有 service、config、model、util
- 不新增无必要抽象层
- 不要破坏现有 REST API
- 不要破坏 `/api/v1/rag/query`
- 不要破坏 `/api/v1/rag/chat`
- 不要破坏 SSE 流式输出行为
- 不要破坏 Token 统计逻辑
- Redis 不可用时，主业务仍应可运行
- 不要为了修复启动问题而粗暴删除缓存条件装配
- 不要硬编码 API Key
- 不要让测试依赖真实外部模型、真实 Milvus、真实 Redis，除非当前启动验证确实必须连接这些依赖

第四阶段：每次修复后的输出格式
每解决一个问题后，输出：
1. 当前问题现象
2. 根因
3. 修改了哪些文件
4. 为什么这样改
5. 如何验证修复有效
6. 当前是否还有新的阻塞问题

第五阶段：最终总结
在本轮启动测试结束后，输出一份完整总结，包含：
1. 实际运行过的命令
2. 已成功启动的服务
3. 仍未启动成功的服务
4. 每个失败点的根因
5. 已修改文件清单
6. 剩余阻塞项
7. 建议下一步如何继续推进

额外要求：
- 优先基于真实代码和实际运行结果做判断
- 遇到不确定点，先搜索项目代码和配置，再决定是否修改
- 如果某个外部依赖缺失，明确说明它是“代码问题”还是“本地环境问题”
- 不要跳过复测
- 在整个过程中保持修改范围尽量小