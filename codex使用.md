先让它“只读分析”，不要一上来就改

先发第一条提示词，不让它立刻写代码：

先不要修改代码。

请阅读当前仓库并完成以下任务：
1. 识别启动这个项目的最小依赖条件
2. 找出各服务的启动顺序
3. 找出本地启动必须的环境变量
4. 找出启动测试时最容易失败的点
5. 给出最小启动验证方案：先启动哪些服务，如何验证它们健康

要求：
- 优先基于真实代码和配置，而不是只看 README
- 输出涉及的配置文件、启动类、端口和依赖关系
- 暂时不要改代码

这样做的原因很简单：Codex 最擅长的是“计划 → 编辑 → 跑测试/构建 → 观察失败 → 修复 → 再验证”的循环；复杂仓库里先让它探路，比直接改更稳。